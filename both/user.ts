import {MeteorObservable, MongoObservable} from 'meteor-rxjs';
import {Meteor} from 'meteor/meteor';

/**
 * 
 * 
 * @export
 * @class User
 */
export class User {
    public name: string;
}

/**
 * Gets the 
 */
